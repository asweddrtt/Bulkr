import 'package:flutter/material.dart';

class AppColors {
  // Primary Neon Greens
  static const Color primaryNeon = Color(0xFFC3F400);      // Used in text headers
  static const Color buttonNeon = Color(0xFFD4FA00);       // Used for filled buttons

  // Off-Whites & Muted Accents
  static const Color offWhiteMuted = Color(0xFFC4C9AC);    // Subtitles and links
  static const Color textGray = Color(0xFF888888);         // Footer text

  // Borders & Dark Accents
  static const Color darkBorder = Color(0xFF333333);       // Outlined button border

  // Surfaces
  static const Color cardDark = Color(0xFF1A1A1A);         // Standard card background
  static const Color cardDeep = Color(0xFF151515);         // Nested / inner boxes
  static const Color navBar = Color(0xFF0D0D0D);           // Bottom navigation bar
}